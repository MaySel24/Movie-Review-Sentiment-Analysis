services:
  - type: web
    name: imdb-sentiment-app
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run streamlit_app_improved.py --server.port=$PORT --server.address=0.0.0.0"
